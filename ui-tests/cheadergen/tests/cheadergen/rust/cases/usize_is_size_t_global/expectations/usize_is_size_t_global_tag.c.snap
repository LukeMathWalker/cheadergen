---
source: ui-tests/cheadergen/src/lib.rs
assertion_line: 332
expression: output
---
#include <stdarg.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>

enum BigEnum {
  A,
  B,
};
typedef size_t BigEnum;

enum SignedEnum {
  Neg = -1,
  Zero = 0,
};
typedef ptrdiff_t SignedEnum;

struct Buffer {
  uint8_t *data;
  size_t len;
  ptrdiff_t offset;
};

typedef size_t ItemCount;

typedef ptrdiff_t Offset;

extern const size_t MAX_LEN;

struct Buffer make_buffer(size_t len, ptrdiff_t offset);

ItemCount buffer_size(struct Buffer buf);

Offset buffer_offset(struct Buffer buf);

size_t classify_big(BigEnum big, SignedEnum signed_enum);
