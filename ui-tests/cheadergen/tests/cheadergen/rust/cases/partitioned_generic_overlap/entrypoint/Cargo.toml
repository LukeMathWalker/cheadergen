[package]
name = "partitioned-generic-overlap"
version = "0.1.0"
edition.workspace = true

[lints]
workspace = true

[dependencies]
partitioned-generic-overlap-dep = { path = "../dep" }
partitioned-generic-overlap-dep2 = { path = "../dep2" }
