---
source: ui-tests/cheadergen/src/lib.rs
assertion_line: 332
expression: output
---
/* WARNING: this file was auto-generated by cheadergen from `export_opaque`. Do not edit it manually. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>

/**
 * A struct exported as opaque — only a forward declaration should appear,
 * not the full definition, despite `#[repr(C)]` and by-value usage.
 */
typedef struct OpaqueConfig OpaqueConfig;

/**
 * A non-repr(C) type exported as opaque — should also produce a forward declaration.
 */
typedef struct OpaqueHandle OpaqueHandle;

/**
 * A struct exported normally — full definition should appear.
 */
typedef struct FullConfig {
  uint32_t width;
  uint32_t height;
} FullConfig;

/**
 * A struct that embeds an opaque type by value — the generated header won't
 * compile because `OpaqueConfig` is an incomplete type.
 */
typedef struct ContainsOpaque {
  uint32_t label;
  struct OpaqueConfig config;
} ContainsOpaque;

/**
 * Takes an opaque type by value — invalid C (incomplete type as parameter).
 */
uint32_t consume_opaque(struct OpaqueConfig config);

/**
 * Takes a struct containing an opaque type by value.
 */
uint32_t consume_contains_opaque(struct ContainsOpaque container);

/**
 * Returns an opaque type by value — invalid C (incomplete return type).
 */
struct OpaqueConfig produce_opaque(void);
