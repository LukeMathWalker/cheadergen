[package]
name = "ui-tests"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
fs-err = "3"
insta = "1"
tempfile = "3"

[[bin]]
name = "ui-tests"
path = "src/main.rs"

[build-dependencies]
toml = "1.0"
serde = { version = "1", features = ["derive"] }

